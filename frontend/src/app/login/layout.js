import '../globals.css';
import './login.css';

export const metadata = {
    title: 'Sign In — Shellkode HR Scheduler',
    description: 'Sign in to your Shellkode HR workspace',
};

export default function LoginLayout({ children }) {
    return children;
}
